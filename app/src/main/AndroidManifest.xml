<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marks.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@drawable/home"
        android:label="yourHealth"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Diseases" />
        <activity android:name=".Kwashakor" />
        <activity android:name=".marasmus" />
        <activity android:name=".BeriBeri" />
        <activity android:name=".Ricket" />
        <activity android:name=".Goiter"></activity>
    </application>

</manifest>